/*let canvas = document.getElementById("canvas"); //elemento <canvas>
let ctx = canvas.getContext("2d"); //GetContext () es un objeto HTML integrado, con propiedades y métodos para dibujar
let canvasWidth = canvas.width; 
let canvasHeight = canvas.height;

ctx.fillStyle = "rgb(23, 88, 166)";  //color CSS, un degradado o un patrón
ctx.fillRect(100, 100, 300, 200); //x, y, width, height*/

let canvas = document.getElementById("canvas"); //elemento <canvas>
let ctx = canvas.getContext("2d"); //GetContext () es un objeto HTML integrado, con propiedades y métodos para dibujar
let canvasWidth = canvas.width; 
let canvasHeight = canvas.height;

ctx.fillStyle = "rgb(85, 0, 0)";  //color CSS, un degradado o un patrón
ctx.fillRect(75, 100, 50, 50); //x, y, width, height
ctx.fillStyle = "rgb(212, 154, 106)";  //color CSS, un degradado o un patrón
ctx.fillRect(50, 150, 50, 50); //x, y, width, height
ctx.fillStyle = "rgb(2, 154, 234)";  //color CSS, un degradado o un patrón
ctx.fillRect(100, 150, 50, 50); //x, y, width, height
ctx.fillStyle = "rgb(122, 5, 103)";  //color CSS, un degradado o un patrón
ctx.fillRect(25, 200, 50, 50); //x, y, width, height
ctx.fillStyle = "rgb(4, 17, 222)";  //color CSS, un degradado o un patrón
ctx.fillRect(75, 200, 50, 50); //x, y, width, height
ctx.fillStyle = "rgb(79, 157, 103)";  //color CSS, un degradado o un patrón
ctx.fillRect(125, 200, 50, 50); //x, y, width, height